# Base URL of the backend REST API (read server-side only).
API_URL=http://localhost:3000

# Serve every page from the hardcoded fixtures in '/mocks' instead of the real API.
API_MOCK=false

# Public address the site itself is served from, used for canonical links, the sitemap and robots.
SITE_URL=http://localhost:3001
